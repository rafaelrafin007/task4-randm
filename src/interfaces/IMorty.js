// IMorty.js - interface documentation (not enforced at runtime)
// A Morty implementation must export a class with at least these methods:
// constructor(gameApi) - receives an object with requestFairNumber(N) and mortyLog(message)
// name() -> string
// getTheoreticalProbabilities(N) -> { switch: Number, stay: Number }
// chooseRemovals(state) -> array of indices to remove (length N-2)
// (Optionally) onRoundEnd(info)


module.exports = {};